#!/usr/bin/env sh

set -eu

readonly rootDir=$(realpath $(dirname $(realpath $0))/../..)

${rootDir}/bin/ci/phpcs
${rootDir}/bin/ci/phpdd
${rootDir}/bin/ci/phpstan
${rootDir}/bin/ci/composer-require-checker
${rootDir}/bin/ci/composer-validate
