#!/usr/bin/env bash

set -eu

readonly ROOT_DIR="$(realpath "$(dirname "$(realpath "$0")")/../..")"
readonly DOCKER_IMAGE_NAME="steevanb/php-parallel-process:release"
readonly DOCKER_FILE_PATH="${ROOT_DIR}/docker/release"

source "${ROOT_DIR}/bin/docker.inc.bash"
